# curlygate-walkthrough
Small walkthrough to acquire the IoCs from a Curlygate variant InfoStealer

The steps in Walkthrough.doc details and breaks down the steps in order to go through several deobfuscation layers to acquire the IoCs such as API key and C2 domains which are the primary ones. Other IOCs (like JS files, folders, data) can be obtained from the decoded JavaScript files.
